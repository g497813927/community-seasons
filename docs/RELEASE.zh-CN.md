# 发布交接

[English](RELEASE.md) | [简体中文](RELEASE.zh-CN.md)

- 现有 Toy 作品：四季共建 · Community Seasons
- Toy ID：30703115016192
- Slug：community-seasons
- 访问方式：PASSWORD。原密码不会保存在本仓库中。

最近一次内容更新于 2026-09-14 提交：重复跳跃输入现在会保留一次完整的跳跃轨迹，不会重新起跳或排队追加跳跃。落地后的新输入会开始下一次跳跃；重复滑铲、跳跃与滑铲之间的切换仍保持原有行为。Toy 接受提交时返回 `auditing` 状态，后续查询确认访问方式仍为 PASSWORD。此次更新未传入元数据或密码参数，因此保留了标题、slug、封面、图标和原密码。

提交前，正式构建、248 项回归测试、类型检查，以及全部六个完整模糊测试套件的两轮执行均已通过。运行 `node run.mjs full --rounds 2 --seed 20260914 --no-tui` 可复现这两轮完整测试（轮次种子为 `20260914` 和 `2674696675`，每轮 224 个渲染种子，共渲染 48,129 帧）。正式源码哈希值保持不变。四项隔离的本地正式版浏览器冒烟检查覆盖了英语/中文和手机/桌面尺寸。托管游戏资源与已测试的构建一致；HTML 的差异仅来自 Toy 包装页的重定向。本地报告和精确构建哈希值位于 `results/publish-validation-jump-2026-09-14T06-40-25Z/`（由 Git 忽略）。

只构建主游戏项目。检查正式版本边界测试，并依据当前 Toy CLI/技能说明执行内容预检。仅更新内容时，省略标题、slug、封面、图标和密码字段即可保留原值。不得通过创建新的公开 Toy 作品或修改可见性来绕过缺失的密码。

完整的许可 JSON 必须位于游戏预期的相对资源路径。Toy 预览可以提供该 JSON，但单独导出的 `THIRD-PARTY-NOTICES.txt` 返回 404；上一次 Toy 上传省略了这个游戏未使用的 TXT 文件。本地和自托管分发仍需保留该文件。游戏界面没有下载链接，所有许可声明均从 JSON 读取。

本仓库未配置自动部署或 GitHub 发布工作流。源码、测试页面、QA URL 控制、生成的录屏和认证文件不得进入正式上传产物。
