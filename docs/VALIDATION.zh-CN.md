# 工作区验证记录 — 2026-09-11

[English](VALIDATION.md) | [简体中文](VALIDATION.zh-CN.md)

在本目录安装其独立的锁定依赖后，完成了以下验证：

- 正式版类型检查与构建：通过。
- 确定性回归测试：242/242 通过。
- 带类型检查的 fast-check 工厂：通过。
- 一轮快速模糊测试：全部六个套件通过（引擎、经济系统、引擎属性、存档属性、类型化生成器和渲染器）。
- 许可面板浏览器 QA：15/15 个场景通过，覆盖英语/中文、窄屏、200% 面板文字大小、延迟加载、焦点、无超链接及暂停行为。
- 聚焦矿车问题的测试页面与扩展 iPhone 测试页面构建：通过。
- 矿车辅助工具、iPhone 探针及测试套件的单元测试：通过。
- 游戏实现及资源与原工作区逐字节比对一致；哈希值记录在 `WORKSPACE_IMPORT.json` 中。

浏览器检查通过 `PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH` 使用本机 Chrome。便携配置默认使用 `npx playwright install chromium` 安装的 Chromium。此次工作区准备没有进行真机手机测试或远程发布。

构建仍会显示原有的产物体积较大提示。一个过时的延迟引用测试夹具已扩展为包含会话题目牌组。过时的字体/DPR 实验和与本仓库无关的 Relic Rush 保留检查未被导入。游戏行为没有改动。
